
export const PLAYGROUND_LENGTH = 30;
export const PLAYGROUND_WIDTH = 30;
export const BOMBCOUNT = 150;
export const SPEED = 200;
export const FPS = 60;

export const GAP_SIZE = 0.01;
export const CELL_SIZE = 20;
export const PADDING = 30;
export const CANVAS_WIDTH = this.PLAYGROUND_LENGTH * (this.CELL_SIZE + this.GAP_SIZE) + this.PADDING;
export const CANVAS_HEIGHT = this.PLAYGROUND_WIDTH * (this.CELL_SIZE + this.GAP_SIZE) + this.PADDING;